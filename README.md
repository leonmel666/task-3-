# Table Processor

Библиотека для обработки табличных данных с поддержкой форматов CSV, Pickle и текстовых файлов.

## 📋 Оглавление

- [Особенности](#особенности)
- [Технологии](#технологии)
- [Установка](#установка)
- [Быстрый старт](#быстрый-старт)
- [Структура API](#структура-api)
- [Примеры использования](#примеры-использования)
- [Архитектура](#архитектура)
- [Тестирование](#тестирование)
- [Лицензия](#лицензия)

## ✨ Особенности

- **Поддержка форматов**: CSV, Pickle, текстовые файлы
- **Типизация данных**: автоматическое определение типов (int, float, bool, str, datetime)
- **Многозадачность**: загрузка и сохранение в несколько файлов
- **Арифметические операции**: сложение, вычитание, умножение, деление столбцов
- **Обработка ошибок**: детальная валидация и исключения
- **Гибкое API**: параметры по умолчанию, именованные аргументы

## 🛠 Технологии

- **Python 3.8+** - основной язык программирования
- **csv** - стандартный модуль для работы с CSV
- **pickle** - сериализация объектов Python
- **datetime** - работа с датами и временем
- **typing** - аннотации типов для лучшей читаемости кода
- **pathlib** - кросс-платформенная работа с путями

## 📦 Установка

Клонируйте репозиторий:

```bash
git clone https://github.com/yourusername/table-processor.git
cd table-processor
```

Библиотека не требует дополнительных зависимостей.

## 🚀 Быстрый старт

```python
from table_processor_complete import Table, load_table_csv, print_table

# Загрузка данных
table = load_table_csv('data.csv', auto_detect_types=True)

# Вывод таблицы
print_table(table)

# Арифметические операции
from table_processor_complete import add_columns
result = add_columns(table, "column1", "column2", "sum")
```

## 📚 Структура API

### Основной класс

#### `Table`
Основной класс для представления табличных данных:
```python
table = Table(data, columns, column_types)
```

### Загрузка данных

#### `load_table_csv()`
Загрузка из CSV файла(ов):
```python
table = load_table_csv('file.csv', auto_detect_types=True)
table = load_table_csv('part1.csv', 'part2.csv')  # из нескольких файлов
```

#### `load_table_pickle()`
Загрузка из Pickle файла(ов):
```python
table = load_table_pickle('data.pickle')
```

### Сохранение данных

#### `save_table_csv()`
Сохранение в CSV:
```python
save_table_csv(table, 'output.csv')
save_table_csv(table, 'output.csv', max_rows=100)  # с разбивкой
```

#### `save_table_pickle()`
Сохранение в Pickle:
```python
save_table_pickle(table, 'data.pickle')
```

#### `save_table_text()`
Сохранение в текстовый файл:
```python
save_table_text(table, 'table.txt')
```

### Операции с таблицами

#### Основные операции
- `get_rows_by_number()` - получение строк по номерам
- `get_rows_by_index()` - получение строк по индексам
- `get_column_types()` - получение типов столбцов
- `set_column_types()` - установка типов столбцов
- `concat()` - объединение таблиц
- `split()` - разделение таблицы

#### Работа со значениями
- `get_values()` - получение значений столбца
- `get_value()` - получение значения из одной строки
- `set_values()` - установка значений столбца
- `set_value()` - установка значения в одну строку

#### Арифметические операции
- `add_columns()` - сложение столбцов
- `sub_columns()` - вычитание столбцов
- `mul_columns()` - умножение столбцов
- `div_columns()` - деление столбцов

#### Визуализация
- `print_table()` - вывод таблицы в консоль

## 📊 Примеры использования

### Пример 1: Базовые операции
```python
from table_processor_complete import *

# Создание таблицы
data = [
    ["id", "name", "age", "salary"],
    [1, "Alice", 25, 50000.50],
    [2, "Bob", 30, 60000.75]
]
table = Table(data[1:], data[0])

# Автоматическое определение типов
table = auto_detect_column_types(table)

# Вывод
print_table(table)

# Получение строк
first_two = get_rows_by_number(table, 0, 2)
```

### Пример 2: Работа с файлами
```python
# Загрузка из нескольких CSV файлов
table = load_table_csv('data_part1.csv', 'data_part2.csv', 
                      auto_detect_types=True)

# Сохранение с разбивкой на файлы
save_table_csv(table, 'output.csv', max_rows=1000)

# Сохранение в текстовом формате
save_table_text(table, 'table_view.txt')
```

### Пример 3: Арифметические операции
```python
# Создание числовой таблицы
data = [
    ["x", "y"],
    [10, 20],
    [15, 25],
    [20, 30]
]
table = Table(data[1:], data[0])
table = auto_detect_column_types(table)

# Выполнение операций
result = add_columns(table, "x", "y", "sum")
result = mul_columns(result, "x", "y", "product")
```

## 🏗 Архитектура

### Алгоритм работы

1. **Загрузка данных**:
   - Чтение файла(ов) формата CSV/Pickle
   - Парсинг строк и преобразование в структуру Table
   - Автоматическое определение типов данных (опционально)

2. **Обработка данных**:
   - Валидация типов и значений
   - Преобразование данных согласно типам столбцов
   - Выполнение операций над таблицей

3. **Сохранение данных**:
   - Преобразование Table в целевой формат
   - Разбивка на файлы при необходимости
   - Запись на диск

### Структура данных Table

```python
Table {
    _data: List[List[Any]]        # Данные таблицы
    _columns: List[str]           # Заголовки столбцов
    _column_types: Dict[Union[int, str], type]  # Типы данных
}
```

### Алгоритм автоматического определения типов

1. **Анализ столбца**:
   - Проверка всех значений на соответствие типам
   - Подсчет успешных преобразований для каждого типа

2. **Определение типа**:
   ```
   Если >80% значений - datetime → datetime
   Если 100% значений - целые числа → int
   Если 100% значений - числа с плавающей точкой → float
   Если 100% значений - булевы значения → bool
   Иначе → str
   ```

3. **Применение типов**:
   - Создание словаря типов столбцов
   - Преобразование всех значений к выявленным типам

## 🧪 Тестирование

### Пример теста
```python
def test_table_operations():
    data = [["id", "value"], [1, 10], [2, 20]]
    table = Table(data[1:], data[0])
    
    # Тест создания
    assert len(table) == 2
    assert table.columns == ["id", "value"]
    
    # Тест операций
    result = add_columns(table, "id", "value")
    assert result is not None
```

Запустите демонстрационный скрипт:
```bash
python table_processor_complete.py
```

## 🔧 Особенности реализации

### Обработка ошибок
- Валидация индексов и имен столбцов
- Проверка совместимости типов при операциях
- Обработка ошибок чтения/записи файлов
- Детализированные сообщения об ошибках

### Производительность
- Использование встроенных типов Python
- Минимизация копирования данных
- Эффективные алгоритмы определения типов

### Расширяемость
- Четкое разделение модулей
- Легко добавлять новые форматы файлов
- Простое добавление новых операций
